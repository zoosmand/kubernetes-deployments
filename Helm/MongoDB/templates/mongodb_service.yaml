apiVersion: v1
kind: Service
metadata:
  name: mongodb-net
  labels:
    app: mongodb
spec:
  selector:
    app: mongodb
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
      nodePort: 30001
      